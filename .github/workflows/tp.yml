name: yeahyeah
on:
  push

jobs:
  execute_binary_windowscringemax:
    runs-on:  windows-latest
    steps:
      - name: verify binary execution on cringdows
        uses: actions/checkout@master

      - name: build and execute
        run: |
          git clone https://github.com/jamailia-pm/sobel_morpho.git
          winget --version
          winget install cmake --accept-package-agreements --accept-source-agreements
          make
          cd bin/make/
          make
          cd Release
          sobel.exe